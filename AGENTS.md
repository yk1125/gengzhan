# 项目协作规则

本仓库是耘栈科技官网，当前产品为 `frontend/` 下的 Vue 3 / Vite 前端。用户最新明确指令优先于本文件及技能中的建议。

## 首次进入

1. 检查 `git status --short`、当前分支及工作树，保留用户现有改动。
2. 读取 `docs/frontend-rebuild/README.md`、当前任务对应的PRD/spec/PLAN，以及 `docs/frontend-rebuild/handoffs/INTEGRATION.md`。
3. 明确任务ID、基准commit、可修改文件、验证方法；只执行已授权阶段。
4. 1.0方案已于2026-09-14获用户整体确认，产品实现未开始。新任务可通过上述文档接手，不依赖原规划对话；按明确任务指令执行，不重复索要方案批准。首页＋服务样板仍需按已约定流程验收。

## 不变量

- 保留现有Vue/Vite路线，本次只改前端；后台缺口用显式mock和BACKEND-TODO，不擅自扩建CMS/API/数据库。
- 中文旧路由保留，英文 `/en/...`；七类服务共享模板，主导航只列四类服务。
- 所有主题/语言/导航/咨询入口统一管理，页面不得另建状态或重复外壳。
- 当地时间19:00—07:00暗，两态按钮；手动选择到下一个边界到期，不能照抄源站08:00规则。
- 客户墙24个独立Logo固定换序；解决方案六分类33企业、28有效外链、5文字占位；不得捏造空链接目标。
- 正式表单真实接收才能报成功。mock仅明确预览，生产不静默fallback到演示数据。
- 不引入参考站的客服、统计、表单端点、联系方式或备案内容。
- 现有公司数据已由用户确认，按文档使用；不要自行增加业绩、电话、地址或二维码。

## 并行协作

- 按SESSIONS.md隔离worktree，最多四个session；共享文件由A负责。
- package/lock、router、全局样式、公共组件、repository/adapter、语言注册只有A修改。
- B拥有首页/公司/联系/法律；C拥有服务模板/配置；D拥有案例/资讯和其样本译文；A拥有AiConsultation与NotFound页面。A在T01初始化最小双语demo内容以供样板使用，然后将该内容目录移交D，不能并行争写。
- 需要改共享契约时先写具体变更与影响，交A统一实现；不要各页面绕开契约。
- T01基础冻结后才能实现首页/服务样板；用户批准样板后再并行定稿其余页面。前置未到时可继续只读调查、内容整理和非依赖工作。
- 不自行创建用户可见的新任务。用户要求多个session的协作规范不等于要求立即创建它们。

## 实现与验证

- 搜索优先rg，读取页面/接口实际用法再改，旧未使用页面不自动当内容来源。
- 新全局设计token集中管理；不得继续叠加全局!important覆盖来掩盖结构错误。
- API解析在adapter，UI文案在双语模块；危险HTML统一清洗，未找到ID显示缺失态。
- 当前只读验证在frontend运行 `npm.cmd run build` 和本地eslint命令，见ACCEPTANCE。
- 现有 `npm run lint` 含 `--fix`，不是只读检查。基线为7 errors/846 warnings；不要把它们误归到新改动，也不能降级规则掩盖错误。
- T01添加测试脚本前不得声称 `test:unit/test:e2e` 可运行或已通过。写有价值的主题、路由、表单、adapter边界与核心旅程测试。
- 修改后运行与范围相符的验证；发现失败修复本任务问题并记录存量问题，成功后避免无理由重复全套测试。
- 源站证据、拟议实现、用户批准样板分开；未经实测的手机/Safari/微信行为标未验证。

## 交接

各session更新自己的 `docs/frontend-rebuild/handoffs/<字母>.md`，A更新PLAN与集成日志。交接必须包含实际提交/文件、命令与结果、截图/录像、接口缺口、未完成事项与下次第一步。

前端实现、外部接口联调、公开上线分开报告。本次规划不触发部署；用户后续明确上线指令按实际配置执行。不要覆盖用户环境文件、提交密钥、清理他人未提交内容或通过破坏性Git操作解决冲突。
