VITE_CONTENT_MODE=api
VITE_ENABLE_MOCK=false
VITE_MOCK_RESOURCES=cases,news,contact,ai
VITE_SITE_URL=

# Set VITE_CONTENT_MODE=mock only for an explicit preview build. Production should use api.
# `vite --mode mock-preview` also enables mock when VITE_ENABLE_MOCK=true (legacy compatibility).
