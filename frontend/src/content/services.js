/** Service media register. Remote capture unavailable; placeholders keep layout stable. */
export const serviceMedia = {
  mini: { source: 'placeholder (reference capture unavailable; no asset registered)', file: 'mini-program-journey-placeholder.svg', sha256: 'UNAVAILABLE', status: 'placeholder', note: 'Reference capture failed in this environment; use CSS phone mock until approved asset is supplied.' }
}
